<h3>Editar Categoria:</h3>
<div class="card">
    <div class="card-body">
        <form action="/adm/categorias/salvarEdicao" method="post">
        <input type="hidden" name="id" value="{{categoria._id}}">
    <label for="nome">Nome:</label>
    <input type="text" name="nome" id="nome" class="form-control" value="{{categoria.nome}}">
    <label for="slug">Slug:</label>
    <input type="text" name="slug" id="slug" class="form-control" value="{{categoria.slug}}">
    <br>
    <button type="submit" class="btn btn-success">Salvar</button>
<!--    <a href="/adm/categorias/lista"><button class="btn btn-success">Lista de Categorias</button></a> -->
</form>
    </div>
</div>